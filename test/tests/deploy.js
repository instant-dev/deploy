const { DeploymentManager } = require('../../index.js');

module.exports = () => {

  describe('DeploymentManager', async () => {

    it('Should find that true is true', async () => {

      expect(true).to.equal(true);

    });

  });

};
